
  # Whitby

  This is a code bundle for Whitby. The original project is available at https://www.figma.com/design/2AJLngis0x6Wrv7xhDnvTd/Whitby.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  